/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel automatically detects Next.js projects and optimizes them
  // No need for 'output: standalone' in dev mode
  // Images and assets will be served from public directory
}

module.exports = nextConfig
